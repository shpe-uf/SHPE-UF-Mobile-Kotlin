
# to find the query u need to look at what u called the query in this case "ExampleQuery"

mutation RegisterMutation($registerInput: RegisterInput) {
    register(registerInput: $registerInput) {
        id
    }
}
